import 'package:flutter/material.dart';

class Appbar extends StatelessWidget {
  const Appbar({super.key, required pagetitle});
   @override
  Widget build(BuildContext context) {
    return SliverAppBar(

    );
  }
}